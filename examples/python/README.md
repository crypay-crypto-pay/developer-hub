# Python Example

> **Coming in Phase 2.**

A Python (`requests`) example for creating payments and verifying webhooks will be added here.

In the meantime, see the [curl example](../curl/) for the raw HTTP calls, or read the [API docs](../../docs/api/).
